export default function removePivot(){
    const selectPivotA = document.querySelectorAll(".pivotA");

    selectPivotA.forEach((select) => {
        select.classList.remove("pivotA"),
        select.classList.add("A")
    })

    const selectPivotAs = document.querySelectorAll(".pivotAs");

    selectPivotAs.forEach((select) => {
        select.classList.remove("pivotAs"),
        select.classList.add("As")
    })

    const selectPivotB = document.querySelectorAll(".pivotB");

    selectPivotB.forEach((select) => {
        select.classList.remove("pivotB"),
        select.classList.add("B")
    })

    const selectPivotC = document.querySelectorAll(".pivotC");

    selectPivotC.forEach((select) => {
        select.classList.remove("pivotC"),
        select.classList.add("C")
    })

    const selectPivotCs = document.querySelectorAll(".pivotCs");

    selectPivotCs.forEach((select) => {
        select.classList.remove("pivotCs"),
        select.classList.add("Cs")
    })

    const selectPivotD = document.querySelectorAll(".pivotD");

    selectPivotD.forEach((select) => {
        select.classList.remove("pivotD"),
        select.classList.add("D")
    })

    const selectPivotDs = document.querySelectorAll(".pivotDs");

    selectPivotDs.forEach((select) => {
        select.classList.remove("pivotDs"),
        select.classList.add("Ds")
    })

    const selectPivotE = document.querySelectorAll(".pivotE");

    selectPivotE.forEach((select) => {
        select.classList.remove("pivotE"),
        select.classList.add("E")
    })

    const selectPivotF = document.querySelectorAll(".pivotF");

    selectPivotF.forEach((select) => {
        select.classList.remove("pivotF"),
        select.classList.add("F")
    })

    const selectPivotFs = document.querySelectorAll(".pivotFs");

    selectPivotFs.forEach((select) => {
        select.classList.remove("pivotFs"),
        select.classList.add("Fs")
    })

    const selectPivotG = document.querySelectorAll(".pivotG");

    selectPivotG.forEach((select) => {
        select.classList.remove("pivotG"),
        select.classList.add("G")
    })

    const selectPivotGs = document.querySelectorAll(".pivotGs");

    selectPivotGs.forEach((select) => {
        select.classList.remove("pivotGs"),
        select.classList.add("Gs")
    })
}